CREATE TABLE HR.STUDENTS (
Name VARCHAR2(100),
Marks number(3)
);

INSERT ALL 
	INTO HR.STUDENTS VALUES ('Ahmed',70)
	INTO HR.STUDENTS VALUES ('Mahmoud',90)
	INTO HR.STUDENTS VALUES ('Ali',50)
	INTO HR.STUDENTS VALUES ('Alaa',30)
	INTO HR.STUDENTS VALUES ('Wesam',20)
SELECT * FROM dual;

SELECT * FROM HR.STUDENTS s CASE
	WHEN s.MARKS >= 90 THEN grades= 'A'
	ELSE 0
END;

SELECT s.*,
    CASE
        WHEN s.MARKS >= 90 THEN 'A'
        WHEN s.MARKS BETWEEN 80 AND 89 THEN 'B'
        WHEN s.MARKS BETWEEN 70 AND 79 THEN 'C'
        ELSE 'F'
END AS GRADE FROM HR.STUDENTS s;
